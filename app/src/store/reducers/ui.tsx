import * as uiActions from "../actions/ui";

const initialState: any = {};

const reducer = (state = initialState, action: any) => {
	const { payload } = action;

	// if (action.type === uiActions.action) {

	// }
	return state;
};

export default reducer;
